import type { Metadata } from 'next'; import { PageHero, PageShell, MockNotice } from '@/components/site-chrome'; import { AppealWorkflow } from '@/components/workflows';
export const metadata: Metadata = { title: 'File a first RTI appeal online | RTI.gov', description: 'Prepare a no-fee first appeal for delay, denial, incomplete information or unreasonable fees.' };
export default async function AppealPage({ searchParams }: { searchParams: Promise<{ registration?: string }> }){const { registration } = await searchParams; return <PageShell><PageHero eyebrow="Submit first appeal" title="Online RTI first appeal form" intro="Retrieve the original online request, select the ground for appeal, add the appeal text and submit without a fee."/><MockNotice/><AppealWorkflow initialRegistration={registration}/></PageShell>}
