import type { Metadata } from 'next'; import { PageHero, PageShell, MockNotice } from '@/components/site-chrome'; import { AuthorityFinder } from '@/components/workflows';
export const metadata: Metadata = { title: 'Find the correct public authority | RTI.gov', description: 'Find the Central, State or local public authority likely to hold the records you need.' };
export default function AuthoritiesPage(){return <PageShell><PageHero eyebrow="Public-authority finder" title="Send the request where the record lives." intro="Search by subject or service. RTI.gov explains whether the authority is Central, State or local before you pay."/><MockNotice>The authority directory is a focused demonstration set. Production would use a verified national authority registry.</MockNotice><AuthorityFinder/></PageShell>}
