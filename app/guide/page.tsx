import type { Metadata } from 'next'; import Link from 'next/link'; import { PageHero, PageShell } from '@/components/site-chrome';
export const metadata: Metadata = { title: 'RTI citizen guide | RTI.gov', description: 'A step-by-step citizen guide to research, write, file, track and appeal an RTI request.' };
export default function GuidePage(){const steps=[['Research first','Search proactive disclosures and the authority website before filing.'],['Find the record holder','Choose the public authority that created, received or controls the record.'],['Describe records precisely','Name the document, subject, location, period and preferred format.'],['Protect your privacy','Do not include Aadhaar, PAN, bank, card or unrelated personal information.'],['Pay once and save proof','Use the approved gateway, save the acknowledgement and never repeat a pending payment.'],['Track the statutory clock','Watch transfers, additional fees, supporting-document requests and the response due date.'],['Appeal when needed','File the first appeal against delay, denial, incomplete information or unreasonable fees.']];return <PageShell><PageHero eyebrow="Citizen guide" title="Seven steps from curiosity to accountability." intro="A modern public service should teach the process in minutes—not require a 29-page manual." actions={<Link className="button-primary" href="/request">Start the guided request</Link>}/><section className="guide-steps">{steps.map(([title,copy],index)=><article key={title}><span>{String(index+1).padStart(2,'0')}</span><div><h2>{title}</h2><p>{copy}</p></div></article>)}</section></PageShell>}
