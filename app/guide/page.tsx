import type { Metadata } from 'next'; import Link from 'next/link'; import { PageHero, PageShell } from '@/components/site-chrome';
export const metadata: Metadata = { title: 'RTI citizen guide | RTI.gov', description: 'A step-by-step citizen guide to research, write, file, track and appeal an RTI request.' };
export default function GuidePage(){const steps=[['Research first','Search Section 4 disclosures, the authority website and open data before filing.'],['Find the record holder','Choose the public authority that created, received or controls the record. The portal sends the case to that authority’s Nodal Officer.'],['Describe records precisely','Name the document, subject, location, period and preferred format.'],['Protect your privacy','Do not include Aadhaar, PAN, bank, card or unrelated personal information.'],['Pay once and save proof','After payment, keep the unique registration number. The Nodal Officer then assigns, splits or transfers the case to the concerned CPIO.'],['Track the statutory clock','Watch the current CPIO, linked registrations, Section 6(3) transfers, additional fees, supporting-document requests and response deadline.'],['Appeal the right decision','File the first appeal against the specific registration number for delay, denial, incomplete information or unreasonable fees.']];return <PageShell><PageHero eyebrow="Citizen guide" title="Seven steps from curiosity to accountability." intro="A modern public service should explain both the citizen action and the government action behind it." actions={<><Link className="button-primary" href="/request">Start the guided request</Link><Link className="button-secondary" href="/process">See how the case moves</Link></>}/><section className="guide-steps">{steps.map(([title,copy],index)=><article key={title}><span>{String(index+1).padStart(2,'0')}</span><div><h2>{title}</h2><p>{copy}</p></div></article>)}</section></PageShell>}
