import type { Metadata } from 'next'; import { PageShell, MockNotice } from '@/components/site-chrome'; import { DemoLogin } from '@/components/workflows';
export const metadata: Metadata = { title: 'Citizen login | RTI Online', description: 'Sign in to the synthetic citizen account with username, password and security code.' };
export default function LoginPage(){return <PageShell><section className="login-page"><MockNotice/><DemoLogin/><aside><span className="page-eyebrow light">Citizen account</span><h1>Login to RTI Online</h1><p>An account is not mandatory for direct filing. Use View History with applicant contact details and OTP when login credentials are unavailable.</p></aside></section></PageShell>}
