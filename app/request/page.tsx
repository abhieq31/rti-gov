import type { Metadata } from 'next'; import { PageShell } from '@/components/site-chrome'; import { RequestWorkflow } from '@/components/workflows';
export const metadata: Metadata = { title: 'Submit an online RTI request | RTI Online', description: 'Complete the Central Government RTI request flow from guidelines and applicant details through payment and registration.' };
export default async function RequestPage({ searchParams }: { searchParams: Promise<{ need?: string; authority?: string }> }){const { need, authority } = await searchParams; return <PageShell><section className="fast-request-head"><div><span>Submit RTI request</span><h1>Online RTI request form</h1></div><p><b>Central Government public authorities only.</b> Read the guidelines, select the ministry and public authority, complete applicant details, pay ₹10 unless BPL-exempt, and save the registration number.</p></section><RequestWorkflow initialNeed={need} initialAuthority={authority}/></PageShell>}
