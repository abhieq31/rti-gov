import type { Metadata } from 'next'; import { PageShell } from '@/components/site-chrome'; import { RequestWorkflow } from '@/components/workflows';
export const metadata: Metadata = { title: 'File an RTI request in under 90 seconds | RTI.gov', description: 'Describe the information, confirm your details and receive a registration number with a clear statutory deadline.' };
export default async function RequestPage({ searchParams }: { searchParams: Promise<{ need?: string; authorityName?: string }> }){const { need, authorityName } = await searchParams; return <PageShell><section className="fast-request-head"><div><span>File an RTI request</span><h1>From question to registration in under 90 seconds.</h1></div><p><b>Independent prototype</b> — nothing is sent or charged. The interaction demonstrates how an official service should work.</p></section><RequestWorkflow initialNeed={need} initialAuthorityName={authorityName}/></PageShell>}
