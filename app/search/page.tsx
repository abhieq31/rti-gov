import type { Metadata } from 'next'; import { PageHero, PageShell, MockNotice } from '@/components/site-chrome'; import { SearchRecords } from '@/components/workflows';
export const metadata: Metadata = { title: 'Search public records before filing RTI | RTI.gov', description: 'Search proactive disclosures, public records and authority topics before making an RTI request.' };
export default function SearchPage(){return <PageShell><PageHero eyebrow="Search public information" title="The fastest RTI request is the one you do not need to file." intro="Search published reports, circulars, tenders, datasets and frequently requested records first."/><MockNotice>Search results are representative synthetic records. A production service would federate public-authority disclosure catalogues.</MockNotice><SearchRecords/></PageShell>}
