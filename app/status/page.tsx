import type { Metadata } from 'next'; import { PageHero, PageShell, MockNotice } from '@/components/site-chrome'; import { StatusLookup } from '@/components/workflows';
export const metadata: Metadata = { title: 'Track RTI request or appeal status | RTI.gov', description: 'Securely view RTI status, deadlines, transfers, replies, additional fees and appeal options.' };
export default async function StatusPage({ searchParams }: { searchParams: Promise<{ registration?: string; email?: string }> }){const { registration, email } = await searchParams; return <PageShell><PageHero eyebrow="Track a request or appeal" title="Status should tell you what happens next." intro="Enter the registration number and applicant email, then use the fixed demo OTP to view a full case timeline."/><MockNotice/><StatusLookup initialRegistration={registration} initialEmail={email}/></PageShell>}
